spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tech_crawler?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: ${MYSQL_PASSWORD:qaz123zz00}
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.crawler.model.entity
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080

# 采集配置
crawler:
  request-delay-ms: 3000
  max-retry: 3
  max-pages: 50

# Claude API
claude:
  api-key: ${CLAUDE_API_KEY:sk-Sqi9J89eJisvVprMeHaWOiArYNkpIaq9NZTj1Dm4XXuXxaJK}
  model: deepseek-v3.2
  base-url: https://api.qingyuntop.top/v1/messages
