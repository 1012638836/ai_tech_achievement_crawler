<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crawler.mapper.SourceSiteMapper">

    <select id="selectPage" resultType="com.crawler.model.entity.SourceSite">
        SELECT * FROM source_site
        <where>
            <if test="school != null and school != ''">
                AND school LIKE CONCAT('%', #{school}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countTotal" resultType="long">
        SELECT COUNT(*) FROM source_site
        <where>
            <if test="school != null and school != ''">
                AND school LIKE CONCAT('%', #{school}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

</mapper>
