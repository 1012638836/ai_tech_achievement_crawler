<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.crawler.mapper.AchievementLinkMapper">

    <select id="selectPage" resultType="com.crawler.model.entity.AchievementLink">
        SELECT * FROM achievement_link
        <where>
            <if test="sourceId != null">
                AND source_id = #{sourceId}
            </if>
            <if test="isAchievement != null">
                AND is_achievement = #{isAchievement}
            </if>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countPageTotal" resultType="long">
        SELECT COUNT(*) FROM achievement_link
        <where>
            <if test="sourceId != null">
                AND source_id = #{sourceId}
            </if>
            <if test="isAchievement != null">
                AND is_achievement = #{isAchievement}
            </if>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

</mapper>
